<!DOCTYPE html>
<html lang="fr">
    <head>
        <%- include("utils/header") %>
    </head>
    <body class="dark:bg-gray-primary bg-white-primary transition duration-500 ease-in-out">
        <div>
            <%- include("utils/navbar") %> 

            <%- include("utils/notif") %>

            <div class="text-center">
                <div class="group">
                    <i class="fab fa-discord font-bold group text-blurple block group-hover:animate-rotate360"></i>
                    <button class="font-bold text-blurple" onclick="location.href='/api/discord/login'">Connexion avec Discord</button>
                </div>
                <div>
                    <button class="font-bold text-transparent gradient-instagram">
                        <i class="fab fa-instagram"></i>
                        
                        Connexion avec Instagram (Bientôt)
                    </button>
                </div>
                <div>
                    <p>Je n'est pas de compte</p>
                </div>
            </div>
        </div>
    </body>
</html>